# Planned Results structure (post-results only)

## 3.1 Common-support retrieval and secondary endpoint baseline

Report executed, evidence-supported common-support outputs and position endpoint scaling as a different-information-condition operational baseline.

## 3.2 Multi-AOI environmental replication

Future placeholders: `Table_MA_AOI_SENSOR_TARGET`, `Figure_MA_AOI_COMPARISON`, `Figure_MA_ENVIRONMENTAL_CONTEXT`. Report AOI-by-sensor-by-target outcomes without a Qinghai-wide representativeness claim.

## 3.3 Rolling-origin forward-year evaluation

Future placeholders: `Table_RO_MATRIX`, `Figure_RO_HISTORY_LENGTH`, `Table_RO_BLOCK_CONTRASTS`. Separate 2024 retrospective results from 2025 primary independent results.

## 3.4 Historical diagnostics versus forward performance

Future placeholders: `Figure_DIAGNOSTIC_FORWARD_ALIGNMENT`, `Table_DIAGNOSTIC_FORWARD_COMPARISON`. Compare diagnostics descriptively and according to the prespecified design.

## 3.5 Secondary endpoint sensitivity

Retain only if it advances operational interpretation. Do not present it as a matched-supervision competition with historical OLS.

No numbers, rankings, significance statements, or empirical conclusions are supplied in this plan.

