# Data

`canonical/paired_observations.csv.gz` is the 681,545-row canonical non-overlapping paired dataset used for final local reproduction.

`metadata/` contains AOI definitions, execution and scene manifests, source-product metadata, and the locked study design. It records the input lineage without redistributing upstream satellite scenes.
