# Environment

Install the verified local reproduction dependencies with `python -m pip install -r environment/requirements.txt`. Exact upstream cloud-extraction credentials are intentionally not part of this repository.
